CREATE TABLE IF NOT EXISTS TODO (TODO_ID INTEGER NOT NULL AUTO_INCREMENT, TITLE VARCHAR(128) NOT NULL, STATUS VARCHAR(32) NOT NULL, DESCRIPTION VARCHAR(4096), updated timestamp, PRIMARY KEY(TODO_ID));